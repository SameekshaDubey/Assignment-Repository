<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Favourite Jokes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Favourite Jokes</h1>
        <div class="row">
            <% jokes.forEach(joke => { %>
                <div class="col-md-4">
                    <div class="card mb-4">
                        <img src="<%= joke.joke_image_url %>" class="card-img-top" alt="Joke Image">
                        <div class="card-body">
                            <p class="card-text"><%= joke.joke %></p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
